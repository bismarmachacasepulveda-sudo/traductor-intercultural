name: traductor
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0
  http: ^1.1.5
  translator: 1.0.3+1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  google_fonts: ^6.1.0 # Para mejores fuentes multilingües
flutter:
  uses-material-design: true
  assets:
    - assets/images/medicamentos/
